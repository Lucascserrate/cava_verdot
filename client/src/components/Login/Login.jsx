import React, {useState} from 'react';
import s from './Login.module.css';

function Login() {

  return (
    <div className={s.login}>
      <form className={`${s.login__content} ${s.container}`}>
        
      </form>
    </div>
  )
}

export default Login